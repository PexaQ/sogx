# Benchmark Routes

Scripted camera routes live here as JSON files and should also be copied or linked into `viewer/public/routes/` for browser access.

Each route should define:

- Coordinate system and units
- Duration
- Keyframes with position, target or rotation, FOV, and optional dwell time
- Keyframe labels for visual inspection

Use the same route for every method on a scene.
