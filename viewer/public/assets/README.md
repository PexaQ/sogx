# Viewer Assets

Scene assets served by the local benchmark server are placed under:

```text
viewer/public/assets/scenes/<scene-name>/
```

Large scene binaries are ignored by git. Metadata for ingested scenes is tracked in `datasets/scenes.json`.
